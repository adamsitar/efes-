       const  {$IFDEF SLOVAK}
                tit_RTZ           = 'Hl senia o stave RTZ prostriedkov';
              {$ENDIF}

              {$IFDEF CZECH}
                tit_RTZ           = 'Hl ¨en¡ o stavu RTZ prost©edk–';
              {$ENDIF}

              {$IFDEF ENGLISH}
                tit_RTZ           = 'Hl senia o stave RTZ prostriedkov';
              {$ENDIF}

